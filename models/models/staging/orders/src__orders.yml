version: 2

sources:
  - name: thelook_ecommerce

    tables:

      - name: order_items
        columns:
          - name:
            tests:
              -

      - name: orders
        columns:
          - name:
            tests: 
              -
